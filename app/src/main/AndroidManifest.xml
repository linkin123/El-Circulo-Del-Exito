<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cinepoilisklic.com.ia.elcirculodelexito">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.menu.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.listaAlumnos.ListaAlumnosActivity" />
        <activity android:name=".ui.activities.reporteAlumno.AsesoriasAlumnoActivity" />
        <activity android:name=".ui.activities.opcionesAlumno.OpcionesActivity" />
        <activity android:name=".ui.activities.altaPaquete.AltaPaqueteActivity" />
        <activity android:name=".ui.activities.registroAlumno.AltaAlumnActivity" />
        <activity android:name=".ui.activities.transicionAlumno.SeleccionaAlumnoActivity" />
        <activity android:name=".ui.activities.listaMaestros.ListaMaestrosActivity" />
        <activity android:name=".ui.activities.seleccionAlumnoMaestro.SeleccionAlumnoMaestroActivity" />
        <activity android:name=".ui.activities.registroMaestro.AltaMaestroActivity"></activity>
    </application>

</manifest>